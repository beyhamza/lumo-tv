<?xml version="1.0" encoding="utf-8"?>
<!--
  Belt and braces next to allowBackup="false": if backup is ever turned on, the
  encrypted session must still stay out of it. Its key lives in this device's
  Keystore and cannot travel, so a restored copy would be undecryptable ballast.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="file" path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="file" path="datastore/" />
    </device-transfer>
</data-extraction-rules>
