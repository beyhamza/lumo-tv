# Recette QA — #215 seul (re-passe) — `fix/S9-05-03-tv-grid-density` @ `7b15dfd`

Re-recette ciblée demandée par @Tech Lead après le correctif Dev `37fe019 → 7b15dfd`
(« la cellule du Guide TV n'affiche que l'heure de début »).
Dépôt `C:\dev\lumo-tv`, base `main` `76c2c42`. Seul `docs/backlog/sprint-09.md` est
modifié (PO) : aucun fichier de code touché → l'APK correspond au SHA.

- APK debug `:app-tv:assembleDebug` (`app-tv-debug.apk`, md5
  `124d50fc68883b18f12f103698006924`) installé sur `Television_1080p`
  (1920×1080, densité 320 → 960×540 dp), `adb reverse tcp:8080 tcp:8080`.
- Compte à froid « Banc à froid mobile » (source `c6463eab…`, 5 chaînes, guide.xml
  du banc avec des programmes le 2026-09-26). Appareil ≈ 12:08 GMT.
- **12 h** = défaut (`en-US`, `time_12_24=null`) ; test complémentaire forcé en
  **24 h** (`time_12_24=24`), puis restauré (`settings delete system time_12_24`).

## Verdict

| # | Point | Verdict |
|---|---|---|
| 1 | **#215** cellule 30 min : horaire = début seul, aucun tiret, aucun « … », 12 h ET 24 h | ✅ **conforme** |
| 2 | En-tête 2 lignes, 5 onglets atteignables au D-pad, onglet sélectionné contrasté | ✅ conforme |
| 3 | Colonne filtres absente en Guide, présente en Chaînes (S9-04-03) | ✅ conforme |
| 4 | 1 seul `/epg` groupé par jour, 0 appel par carte | ✅ conforme |
| 5 | `LEFT` depuis la 1re cellule sort sur le rail (pas de piège) | ✅ conforme |

**#215 est conforme. Aucun défaut résiduel.**

### 1 — #215 : l'horaire ne porte plus que le début (le point)

Cellule 30 min, texte lu au dump : `12:00 PM`, `12:30 PM`, `1:00 PM` — plus de
plage, plus de séparateur. ZOOM sur la cellule focalisée (jugé sur capture, pas
seulement sur le dump) :

- **12 h** (`01-cell-zoom-12h.png`) : titre `Journal de …` (ellipse du titre long,
  attendue) puis **`12:00 PM` complet** — pas de tiret, pas d'ellipse sur l'heure.
  Avant, cette même cellule en 12 h affichait `12:00 PM – 12:…` (fin coupée).
- **24 h** (`02-cell-zoom-24h.png`) : **`12:00` complet**.

La dépendance 12 h / 24 h est bien supprimée : les deux réglages donnent l'heure
de début entière.

### 2 — En-tête (non-régression) : conforme

- **2 lignes** : onglets de jour sur leur ligne, `Now` / `See channels` en dessous
  (`04-guide-header.png`).
- **5 onglets atteignables au D-pad** : depuis la grille `UP` ×2 → `Today`
  (touches : grille → `See channels` → `Today`), puis `RIGHT` → `Sun 27` →
  `Mon 28` → `Tue 29` (dumps `w1/w2/w2-r1..r4.xml`, capture
  `09-tabs-tue29.png` / `09-tabs-tue29-crop.png`).
- **Sélection contrastée** : `Today` = texte `Surface` (#19181E) sur fond blanc
  (`04-guide-header.png`).

### 3 — Colonne filtres : conforme

- **Absente en Guide** : aucun nœud `All` / `Généraliste…` / `Sport…`
  (`02-guide.xml`).
- **Présente en Chaînes** : `All`, `Généralistes (2)`, `Sport (2)`
  (`03-channels.xml`) — S9-04-03 intact.

### 4 — Réseau : conforme

- **Un seul appel par changement de jour** : sélection de `Mon 28`
  (`from=2026-09-28T00:00Z&to=2026-09-29T00:00Z`) → **1** `GET
  /v1/sources/c6463eab…/epg` avec **les 5 `channelIds`** + **1** réponse 200.
  Log `11-daynav-lumolog.txt`. (Idem constaté sur `Sun 27`.)
- **0 appel par carte** : navigation cellules (`RIGHT RIGHT DOWN RIGHT LEFT
  RIGHT UP RIGHT`) → **0** ligne `LumoHttp`, donc 0 `/epg`. Log
  `15-cellnav-lumolog.txt`.

### 5 — Focus LEFT : conforme

Depuis une cellule de grille, `LEFT` recule d'abord vers les cellules de gauche
(la 1re cellule est celle du programme en cours, fenêtre ancrée sur `now`) ; à
bout de grille, `LEFT` n'est plus consommé et le focus **sort sur le rail**
(fin sur `Settings`, item de rail le plus proche verticalement). Pas de piège
(après 26 `LEFT`, dump `y2.xml`). Capture `11-after-left.png`.

## Unitaires et lint

- `./gradlew :feature:live:testDebugUnitTest --rerun-tasks` → **BUILD SUCCESSFUL**,
  **9 suites / 71 `testcase` / 0 échec** (dont `GuideCellTimeLabelTest` = 2,
  `GuideFocusTest` = 23). Compte annoncé par Dev (9/71/0) **confirmé**.
- `./gradlew :feature:live:lintDebug` → **BUILD SUCCESSFUL**.

Test neuf `GuideCellTimeLabelTest` relu : il épingle `gridCellTimeLabel("12:00 PM",
"12:30 PM") == "12:00 PM"`, sans `–` et sans `12:30` — échoue sur l'ancienne
expression `start + " – " + end`.

## ⚠️ Observations hors critères (non bloquantes)

- **Le focus d'un onglet ne charge pas le jour ; la confirmation (OK / tactile)
  si.** Au D-pad, `RIGHT` déplace le focus sur l'onglet suivant mais **ne
  déclenche aucun `/epg`** ; c'est `OK` sur l'onglet focalisé qui lance l'unique
  requête groupée (`13.xml` focus `Mon 28` sans appel, puis `ok` → 1 appel).
  Le critère « 1 seul `/epg` groupé par page/jour » tient ; à savoir si un test
  e2e attend un fetch sur simple `RIGHT`.
- Fenêtre **ancrée sur `now`** (préexistant, `guideWindow` identique sur `main`) :
  à 12:08 la 1re cellule est un sliver du programme en cours. Non introduit par
  `7b15dfd`.
- Titre long toujours ellipsé (`Journal de …`) — attendu, le titre de banc est
  volontairement long ; le défaut tranché était l'horaire, pas le titre.

## Environnement

- `time_12_24` supprimé (retour au défaut), app `force-stop`.
- `lumo-bench`, `lumo-api`, `lumo-e2e-*` non touchés. Plane et `docs/` non
  modifiés. Rien fusionné (`gh` absent → Hamza merge).
