/**
 *
 * Please note:
 * This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * Do not edit this file manually.
 *
 */

@file:Suppress(
    "ArrayInDataClass",
    "EnumEntryName",
    "RemoveRedundantQualifierName",
    "UnusedImport"
)

package tv.lumo.android.network.generated.model


import com.squareup.moshi.Json
import com.squareup.moshi.JsonClass

/**
 * A series, as a listing carries one.  **A `VodItem` minus one field and plus one.** No `container_extension`, because a series is not played — its episodes are. And an `episode_run_time`, which is indicative rather than authoritative: it is what the panel says a typical episode lasts, not the length of any particular one.  Everything else is a film's, deliberately: `poster_url` nullable, `plot` absent from listings and filled by `GET /series/{id}`, `rating` echoed verbatim. A client that wrote a film card can draw this one.  **No season and no episode here.** They cost a call to the user's own server, per series — see `GET /sources/{id}/series`. 
 *
 * @param id 
 * @param sourceId 
 * @param name 
 * @param position Display order within the source.
 * @param isAdult As the source flags it. Exposed and **not acted on**: filtering it is a parental control, parental control needs profiles, and profiles are v2. 
 * @param categoryId 
 * @param externalId Identifier used by the origin panel.
 * @param posterUrl Artwork advertised by the user's own source. Lumo ships no bundled poster and no fallback of its own; null means a client renders the title, never a picture of ours standing in for one of theirs. 
 * @param year First-broadcast year, when the source states one.
 * @param episodeRunTime Typical episode length in minutes, as the panel states it.  **Indicative, and never used as a duration.** A saved position needs the length of the episode being watched, which is `Episode.duration_seconds`; using this in its place would compute \"finished\" against a number that belongs to no episode in particular. 
 * @param rating Whatever the source calls a rating, echoed verbatim and never reinterpreted — the same ruling as `VodItem.rating`. 
 * @param plot Synopsis, and **absent from the listing on purpose**, exactly as on `VodItem`: it arrives with the tree, from the single call that `GET /series/{id}` makes. 
 */


data class Series (

    @Json(name = "id")
    val id: java.util.UUID,

    @Json(name = "source_id")
    val sourceId: java.util.UUID,

    @Json(name = "name")
    val name: kotlin.String,

    /* Display order within the source. */
    @Json(name = "position")
    val position: kotlin.Int,

    /* As the source flags it. Exposed and **not acted on**: filtering it is a parental control, parental control needs profiles, and profiles are v2.  */
    @Json(name = "is_adult")
    val isAdult: kotlin.Boolean,

    @Json(name = "category_id")
    val categoryId: java.util.UUID? = null,

    /* Identifier used by the origin panel. */
    @Json(name = "external_id")
    val externalId: kotlin.String? = null,

    /* Artwork advertised by the user's own source. Lumo ships no bundled poster and no fallback of its own; null means a client renders the title, never a picture of ours standing in for one of theirs.  */
    @Json(name = "poster_url")
    val posterUrl: kotlin.String? = null,

    /* First-broadcast year, when the source states one. */
    @Json(name = "year")
    val year: kotlin.Int? = null,

    /* Typical episode length in minutes, as the panel states it.  **Indicative, and never used as a duration.** A saved position needs the length of the episode being watched, which is `Episode.duration_seconds`; using this in its place would compute \"finished\" against a number that belongs to no episode in particular.  */
    @Json(name = "episode_run_time")
    val episodeRunTime: kotlin.Int? = null,

    /* Whatever the source calls a rating, echoed verbatim and never reinterpreted — the same ruling as `VodItem.rating`.  */
    @Json(name = "rating")
    val rating: kotlin.String? = null,

    /* Synopsis, and **absent from the listing on purpose**, exactly as on `VodItem`: it arrives with the tree, from the single call that `GET /series/{id}` makes.  */
    @Json(name = "plot")
    val plot: kotlin.String? = null

) {


}

